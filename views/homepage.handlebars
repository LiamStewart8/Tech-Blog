{{#each blog}}
<div class="card" style="width: 36rem;">
    <div class="card-body">
        <h5><a href="/blog/{{id}}" class="card-title">{{title}}</a></h5>
        <h6 class="card-subtitle mb-2 text-muted">{{User.name}}</h6>
        <p class="card-text">{{body}}</p>
    </div>
</div>
{{/each}}